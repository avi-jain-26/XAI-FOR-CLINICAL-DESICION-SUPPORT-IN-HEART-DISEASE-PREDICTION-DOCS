{
  // VS Code run/debug configuration. Press F5 to run the pipeline.
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run: Heart Disease XAI pipeline",
      "type": "debugpy",
      "request": "launch",
      "program": "${workspaceFolder}/src/main.py",
      "console": "integratedTerminal",
      "cwd": "${workspaceFolder}",
      "justMyCode": true
    }
  ]
}
