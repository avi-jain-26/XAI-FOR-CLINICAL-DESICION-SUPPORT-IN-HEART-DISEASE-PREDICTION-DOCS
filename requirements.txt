# Core data / ML stack (matches the methodology in the paper)
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3        # LogisticRegression, RandomForest, SVM, GradientBoosting, StackingClassifier
xgboost>=2.0             # XGBoost base learner

# Explainable AI (XAI)
shap>=0.44               # SHapley Additive exPlanations

# Visualization
matplotlib>=3.7
seaborn>=0.12
networkx>=3.1            # graph-based feature-dependency visualization
